package Tetris.Game {		import flash.display.MovieClip;	import Tetris.Game.Blocks.*;		public class Blocks {		private static var stage;				public static function initialize(stage) {			Blocks.stage = stage;						// Spawn block			var test = new BlockYellow();			test.x = 0;			test.y = 0;						stage.addChild(test);		}				public static function update() {			// Frame		}	}}